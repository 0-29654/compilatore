CORREZIONE COMPILATORE 1.8.5

Errore risolto:
setup_student.iss - Unknown identifier 'SURFACEWIDTH'.

Correzione:
WizardForm.InstallingPage.SurfaceWidth
è stato sostituito con:
WizardForm.InstallingPage.Surface.Width

Restano invariati:
- animazione C++ durante la schermata di installazione;
- overlay interni in modalità verifica;
- main.cpp e file .h;
- rinomina header;
- invio al docente;
- tutte le modifiche client precedenti.

Applicazione:
copiare tutto il contenuto nella repository locale compilatore,
sovrascrivere, quindi fare git add, commit e push.
